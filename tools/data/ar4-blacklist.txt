# wp362-blacklist.txt — фразы-маркеры ИИ-стилистики для AR.4
# Контекст: peer-session 2026-05-28-16 (CONSENSUS гибрид blacklist+whitelist+profile)
# Применение: wp362-style-grep.sh
# Логика: blacklist_count - whitelist_count = AR.4 count
#   - 1-2 совпадений → WARN
#   - ≥3 совпадений → FAIL
# Каждая строка — фраза или regex (без флага -E экранирование не нужно)

# Поверхностные обобщения
ключевое различие
ключевое отличие
ключевое в том что
основной момент в том что
главное здесь в том что

# Презентационные обращения
представьте себе
давайте разберёмся
давайте рассмотрим
давайте посмотрим

# Эпохальные обобщения
в современном мире
в современную эпоху
в эпоху искусственного интеллекта

# Метатекст
важно отметить что
стоит подчеркнуть что
заметим что
рассмотрим следующий пример

# Псевдоавторитет
золотое правило
универсальное правило
непреложный закон

# Тройные параллели (ловятся через regex в wp362-style-grep.sh)
# «не X, не Y, не Z» — не в blacklist, отдельная проверка
