# Анализ раздела 06 по WRITING-PIPELINE

**Дата:** 2026-05-17
**Раздел:** `s6-stages-and-gates` (Ступени и ворота развития)
**Файлы:** `6.01-five-stages.md` … `6.07-transition-plan.md` (7 подразделов)
**Путь в docs:** `docs/ru/personal-design/1-1-systemic-self-development/s6-stages-and-gates/`
**Конвейер:** `specs/v4-reference/WRITING-PIPELINE.md`
**Объём:** 518–652 слова на подраздел (итого ≈ 4038 слов), диапазон 500–1500 ✅

---

## Этап 1. Контекст и источники

**[❌ Критично]**

### 1.1 Pack-шифры в теле текста (нарушение ограничения #1 промпта)

Во всех 7 файлах в секции «Определение из источника» присутствуют Pack-шифры в читаемом тексте:

| Файл | Шифр в тексте |
|------|---------------|
| 6.01 | `PD.FORM.089` |
| 6.02 | `PD.FORM.089 §5.2` |
| 6.03 | `PD.FORM.089 §5.3` |
| 6.04 | `PD.FORM.089 §5.4` |
| 6.05 | `PD.METHOD.008` |
| 6.06 | `PD.FORM.022 §3`, `PD.FORM.089 §5` |
| 6.07 | `PD.METHOD.008 §6` |

Правило: шифры Pack — только во frontmatter, не в тексте. Заменить на описательное название: «В онтологии созидателя», «В методологии личного развития» и т. п.

### 1.2 Отсутствие Pack-источников в `introduces` (A.10 Evidence Graph)

Все поля `introduces` содержат только канонические имена без аннотации Pack-источника:

```yaml
introduces: ["Ступень развития", "Мастерство как главный капитал"]  # ❌ нет (PD.FORM.NNN)
```

Требуется: каждое понятие в `introduces` должно иметь Pack-источник. Линтер `check_evidence_graph` выдаст FAIL.

---

## Этап 2. Технический gate (frontmatter)

**[⚠️ Требует внимания]**

Все обязательные поля присутствуют во всех 7 файлах:
`subsection_id`, `title`, `order`, `introduces`, `uses`, `prerequisites`, `can_do`, `cp_check`, `bh_check`, `bottleneck_hint`, `mastery_node`, `stage_relevant` ✅.

Нарушения:

- **`can_do` в 6.06** содержит служебный термин «bottleneck-метод»:
  ```yaml
  can_do:
    - "Могу определить стадию квадранта для своего bottleneck-метода"  # ❌ → "узкое место"
  ```
  По Приложению Б конвейера: `bottleneck` → «узкое место».

- **`introduces` в 6.01** содержит «Мастерство как главный капитал» — тезис, а не каноническое имя понятия. В Сигнатуре используется просто «Мастерство». Несоответствие.

- **`introduces` в 6.06** содержит «Связь ступеней и квадранта» — описание отношения, а не имя понятия.

---

## Этап 3. Структура (понятия, плотность)

**[⚠️ Требует внимания]**

Плотность понятий соблюдена (1–2 вводимых понятия на подраздел) ✅. Полный набор SS1–SS7 присутствует ✅.

Несоответствия между `introduces` (frontmatter) и «Сигнатура понятий» (тело текста):

| Файл | В `introduces` | В Сигнатуре, но НЕ в `introduces` |
|------|---------------|-----------------------------------|
| 6.04 | `["Узкое место"]` | «Теория ограничений» |
| 6.05 | `["Профориентация"]` | «Выбор ролей» |
| 6.06 | `["Связь ступеней и квадранта"]` | «Степень мастерства» |

Либо добавить недостающие понятия в `introduces`, либо убрать их из Сигнатуры. Сигнатура должна отражать только `вводится`-понятия из frontmatter.

---

## Этап 4. Промпт: мем → метод → мировоззрение

**[❌ Критично]**

### 4.1 IWE в руководстве 1 (нарушение ограничения #4)

В файле **6.01** (строка ~30):
> «Пятая — Проактивный — это режим, в котором человек не просто развивается, а проектирует своё развитие как систему, **включая IWE** и сообщество.»

Правило конвейера: «НЕ писать про IWE в руководствах 1 и 2». Заменить: «включая инструменты и сообщество».

### 4.2 Pack-шифры в тексте

Повторяет Этап 1.1 — шифры `PD.FORM.089`, `PD.METHOD.008` и др. в читаемом тексте нарушают ограничение #1 промпта.

### 4.3 Именная ссылка на реального человека (6.04)

В файле **6.04** (Определение из источника):
> «Теория ограничений (Eliyahu Goldratt) применена к личному развитию...»

Запрещено: «Прямые цитаты "реальных людей" с именами». Убрать имя, оставить только название концепции.

### 4.4 Служебный термин «bottleneck» в тексте (6.06, 6.07)

В 6.06 тело: «bottleneck-метод» (4 упоминания), в 6.07 тело: «Bottleneck» (2 упоминания). По Приложению Б: заменить на «узкое место».

### 4.5 Сильные стороны (Этап 4)

- Цепочка мем → метод → мировоззрение прослеживается во всех 7 файлах ✅
- 13-частная структура соблюдена строго (от «В одном предложении» до «Что дальше») ✅
- Тон инструктивный («Вот как устроено»), не императивный ✅
- Личный опыт автора отсутствует ✅
- Образовательная лексика отсутствует ✅
- Обобщённые кейсы без имён автора ✅

---

## Этап 5. Самопроверка (can_do, практика)

**[⚠️ Требует внимания]**

`can_do` во всех файлах начинается с «Могу» ✅. Секция «На практике» присутствует в каждом подразделе ✅.

Проблемы:

- `can_do` в **6.06** содержит «bottleneck-метод» (служебный термин, см. Этап 2).
- `can_do` в **6.07**: «Могу составить план перехода на следующую ступень с конкретными действиями и датой проверки» — составление полного плана за ≤5 мин проблематично. Рекомендуется переформулировать как более атомарное действие.

---

## Этап 6. Платформенная сверка (bh/cp)

**[⚠️ Требует внимания]**

Используемые `cp_check`: `cp.rhy`, `cp.wld`, `cp.skl` — все в списке FORM.089 §4 ✅.

Используемые `bh_check`: `bh.sys`, `bh.met`, `bh.agn`, `bh.inv`.

Вопрос по **`bh.inv`**: в перечне Этапа 6 п. 3 («bh.sys, bh.stb, bh.met, bh.awr, bh.agn, bh.scl») он явно не включён. К4-fix определяет `bh.inv = slot_logged.hours + lesson_completed.duration_minutes/60`, что подразумевает его легитимность. Требуется верификация с актуальной `PD.FORM.089-learner-rcs.md`. Используется в 6.02, 6.03, 6.07.

`stage_relevant` / `cp_check` / `bh_check` — соответствие разумно ✅. Единственный файл с ограниченным `stage_relevant` — 6.05 (`[2, 3, 4, 5]`) — обоснован тематически.

---

## Этап 7. FPF-проверка + нарративный gate

**[❌ Критично]**

### A.10 Evidence Graph — FAIL

`вводится`-понятия во всех файлах не имеют Pack-источников (дублирует Этап 1.2). Линтер `check_evidence_graph` выдаст FAIL.

### A.1.1 Bounded Context — FAIL

IWE в Guide 1 (6.01) — нарушение (дублирует Этап 4.1). Линтер `v4-lint porter` выдаст FAIL.

### A.7 Strict Distinction ✅

Кейсы и аналогии в `introduces` отсутствуют ✅.

### A.11 Ontological Parsimony ✅

≤3 понятий `вводится` на подраздел ✅ (везде ≤2).

### Нарративный gate ✅

Цепочка «мем → метод → мировоззрение» прослеживается ✅. Тональность конкретная (ступени 1–2), без цивилизационных обобщений ✅.

---

## Этап 8. Porter-readiness

**[⚠️ Требует внимания]**

| Поле | Статус |
|------|--------|
| `stage_relevant` заполнен корректно | ✅ |
| `mastery_node` ∈ {мыслительное/саморазвитие/iwe} | ✅ (все = `[саморазвитие]`) |
| `can_do` начинается с «Могу» | ✅ (6.06 ⚠️ — содержит «bottleneck») |
| `introduces` без `U.*` | ✅ |
| `prerequisites` ссылаются на существующие подразделы | ✅ |

Риски для Портного:
- «Мастерство как главный капитал» (6.01) — нестандартное имя понятия, может не совпасть с графом
- «Связь ступеней и квадранта» (6.06) — описание связи вместо имени понятия

Алгоритм Портного формально работоспособен, но два нестандартных имени создают риск ошибки идентификации.

---

## Этап 10. Верификация времени

**[⚠️ Требует внимания]**

| Подраздел | Слов (Python) | Чтение ~150 сл/мин | Декларировано |
|-----------|---------------|--------------------|---------------|
| 6.01 | 652 | ~4 мин | 25 мин |
| 6.02 | 583 | ~4 мин | 25 мин |
| 6.03 | 566 | ~4 мин | 25 мин |
| 6.04 | 596 | ~4 мин | 25 мин |
| 6.05 | 536 | ~4 мин | 25 мин |
| 6.06 | 587 | ~4 мин | 25 мин |
| 6.07 | 518 | ~3 мин | 25 мин |

Во всех 7 файлах одинаковое «25 мин чтение + 15 мин практика» — признак шаблонного проставления без калибровки. Даже при медленном рефлексивном чтении (~50–60 сл/мин) реальное время чтения ≈10–13 мин, что в 2× меньше декларированного. Превышает 20%-й допуск (Этап 12).

**Объём 500–1500 слов:** все файлы в диапазоне ✅. Нижняя граница: 518 слов (6.07) — близко к минимуму, рекомендуется дорастить до 600+.

---

## Итог

**Пройдено этапов (без замечаний):** 0/9
**Условно пройдено (⚠️):** 5/9 (Этапы 2, 3, 5, 6, 8, 10)
**Не пройдено (❌):** 3/9 (Этапы 1, 4, 7)

---

### Критичные проблемы (❌) — блокируют merge:

1. **Pack-шифры в теле текста** (все 7 файлов): `PD.FORM.089`, `PD.METHOD.008`, `PD.FORM.022` в читаемом тексте нарушают ограничение #1 промпта — заменить на описательные названия источников.
2. **IWE в руководстве 1** (6.01, строка ~30): нарушение ограничения #4 — убрать «IWE», заменить на нейтральную формулировку.
3. **Отсутствие Pack-источников в `introduces`** (все файлы): A.10 Evidence Graph FAIL — добавить `(PD.FORM.NNN)` к каждой записи `вводится`.

---

### Требуют внимания (⚠️):

4. **Служебный термин «bottleneck»** в `can_do` и тексте 6.06, 6.07 — заменить на «узкое место».
5. **Именная ссылка «Eliyahu Goldratt»** (6.04) — убрать имя, оставить только название концепции.
6. **Несоответствие `introduces` ↔ Сигнатура** (6.04, 6.05, 6.06): три понятия из Сигнатуры отсутствуют во frontmatter — выровнять.
7. **Нестандартные имена в `introduces`**: «Мастерство как главный капитал» (6.01), «Связь ступеней и квадранта» (6.06) — привести к каноническому виду.
8. **Время чтения не калибровано** (все файлы): одинаковые «25 мин» при реальных ~4 мин — верифицировать секундомером (Этап 12) и скорректировать.
9. **Валидация `bh.inv`**: уточнить наличие в актуальной `PD.FORM.089-learner-rcs.md` (используется в 6.02, 6.03, 6.07).
10. **`can_do` в 6.07** — «составить план перехода» не проверяется за ≤5 мин; переформулировать.

---

### Сильные стороны (✅):

1. **13-частная структура** строго соблюдена во всех 7 подразделах (от «В одном предложении» до «Что дальше»).
2. **Мем → Метод → Мировоззрение** — цепочка читаема и последовательна в каждом файле.
3. **Frontmatter полный**: все обязательные поля (`subsection_id`, `mastery_node`, `stage_relevant`, `can_do`, `cp_check`, `bh_check`, `bottleneck_hint`) присутствуют.
4. **Плотность понятий** (1–2 на подраздел) и объём (518–652 слова) — в допустимом диапазоне.
5. **Нет личного опыта автора**, нет образовательной лексики (кроме указанных нарушений).
6. **4-степенная шкала мастерства** (объясняю → умею → навык → мастерство) — единообразно применена.
7. **Кросс-ссылки** (`§Х.ХХ`) и `prerequisites` — корректны и верифицируемы.
